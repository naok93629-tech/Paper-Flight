export const PIPE_CONFIG = Object.freeze({
    width: 60,
    speed: 2,

    spawnInterval: 100, // tick

    minGapHeight: 80,
    maxGapHeight: 130,

    minGapUpperY: 100,
    maxGapUpperY: 250,
});